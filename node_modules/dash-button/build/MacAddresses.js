'use strict';

var _interopRequireDefault = require('babel-runtime/helpers/interop-require-default').default;

Object.defineProperty(exports, '__esModule', {
  value: true
});

var _lodashPadleft = require('lodash.padleft');

var _lodashPadleft2 = _interopRequireDefault(_lodashPadleft);

let MacAddresses = {
  getEthernetSource: function (packet) {
    return MacAddresses.decimalToHex(packet.payload.shost.addr);
  },

  decimalToHex: function (numbers) {
    let hexStrings = numbers.map(function (decimal) {
      return (0, _lodashPadleft2.default)(decimal.toString(16), 2, '0');
    });
    return hexStrings.join(':');
  }
};

exports.default = MacAddresses;
module.exports = exports.default;